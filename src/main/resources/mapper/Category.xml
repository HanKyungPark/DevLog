<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.bitcamp.devlog.mapper.CategoryMapper">
    <insert id="insertCategory" parameterType="Category">
        insert into category (category_id,category_type,account_id) values (#{categoryId},#{categoryType},#{accountId});
    </insert>
    <select id="getCategoryList" resultType="List">
        select category_id,category_type from category;
    </select>
    <update id="updateCategory" parameterType="Category">
        update category set category_type=#{categoryType} where category_id=#{categoryId};
    </update>
    <delete id="deleteCategory" parameterType="long">
        delete from category where category_id=#{categoryId};
    </delete>
    <select id="findByPostId" parameterType="post" resultType="Category">
        select
            category_type,
            c.category_id as category_id
        from category c Join post p on c.category_id = p.category_id
        where p.post_id = #{postId}
    </select>
</mapper>