<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.bitcamp.devlog.mapper.Account">

    <insert id="save" parameterType="Account">
        insert into account (name,email,provider_type,account_id,a_created_at, file, blog_id, biography, homepage)
        values(#{name},#{email},#{providerType},#{accountId},now(), #{file}, #{blogId}, #{biography}, #{homepage})
    </insert>

    <update id="update" parameterType="Account">
        update account
        set a_update_at= now(),
            biography=#{biography},
            blog_id=#{blogId},
            homapage=#{homepage},
            file=#{file}
    </update>

    <!--블로그 검색-->
    <select id="findAllByBlogId" parameterType="String" resultType="Account">
        select * from account
        where blog_id = #{blogId}
    </select>

</mapper>